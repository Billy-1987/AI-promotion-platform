# dependencies
node_modules/

# Next.js build output
.next/
out/
*.tsbuildinfo

# Runtime data volumes (mapped on server, must not enter git or image)
data/images/
public/generated/

# env files (contains API keys)
.env
.env.local
.env*.local

# bigoffs-deploy: per-project deploy key (ephemeral, never commit)
deploy_key
deploy_key.pub

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
