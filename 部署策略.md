# 部署策略

> 本文档面向**人 + AI**：同事和 AI 助手都按这里描述的流程操作，避免重复挖坑。

## TL;DR

**`git push origin main` = 部署上线**。GitHub Actions 会自动 SSH 到生产服务器、rebuild Docker、重启容器、刷新 nginx。无需手动 ssh、无需跑脚本。

回退请提交 `git revert`，不要直接改服务器文件。

---

## 1. 部署链路

```
本地 git push origin main
        │
        ▼
GitHub Actions (.github/workflows/deploy.yml)
        │  ssh deployer@47.95.109.68
        ▼
/opt/apps/aipp/    ← rsync 后 docker build --no-cache → docker run
        │
        ▼
nginx (server_name: source.aipp.bigoffs.cn aipp.bigoffs.cn, 80+443)
        │
        ▼
用户：https://aipp.bigoffs.cn （CDN） / https://source.aipp.bigoffs.cn （源站）
```

- 触发条件：`push` 到 `main` 分支，或在 Actions 页面手动 `workflow_dispatch`。
- 构建耗时：约 4-5 分钟（npm install + next build + docker rebuild）。
- 容器名固定为 `aipp`，端口 `3001`，nginx 反代到 `127.0.0.1:3001`。
- 持久化卷：`/opt/apps/aipp/data` → `/app/data`、`/opt/apps/aipp/generated` → `/app/public/generated`。这两个目录跨部署保留，不要把生成的图片塞到镜像里。

## 2. 服务器和密钥

| 项 | 值 |
|---|---|
| 区域 | 阿里云 新加坡 ap-southeast-1（Gemini 模型对香港有 region 限制，**不要迁港**） |
| 公网 IP | `47.95.109.68` |
| CI 部署账号 | `deployer`（在 docker 组里，免 sudo） |
| 应急排查账号 | `root`（仅人工排查用，不进 CI） |
| 项目路径 | `/opt/apps/aipp/` |
| 运行时环境变量 | `/opt/apps/aipp/.env`（必含 `MODELVERSE_API_KEY`、`BIGOFFS_CLIENT_ID`、`BIGOFFS_CLIENT_SECRET`） |

### GitHub Secrets（`JeremyDong22/AIPP`）

- `SSH_PRIVATE_KEY`：**专用** ed25519 私钥，仅授权给 `deployer@server`，不复用任何人的个人密钥。
- `SERVER_HOST` = `47.95.109.68`
- `SERVER_USER` = `deployer`
- `REMOTE_DIR` = `/opt/apps/aipp`

要轮换 SSH 密钥：本地生成新 keypair → 用 root SSH 把 pubkey 写到 `/home/deployer/.ssh/authorized_keys`（删掉旧的）→ `gh secret set SSH_PRIVATE_KEY --repo JeremyDong22/AIPP < new_private_key`。

## 3. 验证部署是否真的生效

只看 GitHub Actions 绿勾不够，要拿一个**视觉/数据上能区分的改动**对照线上。流程：

### 3.1 看 CI 状态（**AI：每次部署后必须挂一个 monitor**）

push 完不能立刻报"部署完了"就走。CI 要 4-5 分钟，必须**起一个后台轮询**盯到 `completed success` 再继续验证，失败立即报告日志。推荐 pattern：

```bash
# 1. 取最新 run id
RUN_ID=$(gh run list --repo JeremyDong22/AIPP --workflow=deploy.yml --limit 1 --json databaseId -q '.[0].databaseId')

# 2. 后台轮询（用 Bash 工具的 run_in_background=true，或单独跑 gh run watch）
for i in $(seq 1 12); do
  STATUS=$(gh run view "$RUN_ID" --repo JeremyDong22/AIPP --json status,conclusion -q '.status + " " + (.conclusion // "")')
  echo "[$i] $STATUS"
  case "$STATUS" in completed*) break ;; esac
  sleep 30
done
```

或更简洁：`gh run watch $RUN_ID --repo JeremyDong22/AIPP --exit-status`（阻塞直到完成，失败时退出码非 0）。

**完成后必做**：
- `completed success` → 进入 §3.2 验证容器
- `completed failure` → `gh run view $RUN_ID --repo JeremyDong22/AIPP --log-failed | tail -80` 取错误片段直接给用户，不要假装成功

### 3.2 确认容器是新版本

```bash
ssh root@47.95.109.68 "docker ps --filter name=aipp --format '{{.Status}}'"
```

`Up <秒数> seconds` ＜ 5 分钟 ＝ 刚部署过。如果是 `Up X days` 说明这次 push 没真重启容器。

### 3.3 在容器内部抓改动内容

不要只看 `https://aipp.bigoffs.cn` 的首页 HTML —— 这个项目首页会跳转到 `/calendar`，且 `LoginPage` 是客户端组件，SSR 阶段不渲染按钮文案，光看 HTML 抓不到。最可靠的方式是直接 grep 构建产物：

```bash
ssh root@47.95.109.68 "docker exec aipp grep -roE '<你刚改的字符串>' /app/.next/static 2>/dev/null | head -3"
```

文案改动、组件改名、API 路径变更都能这样验证。

### 3.4 健康检查

```bash
ssh root@47.95.109.68 "curl -sS -o /dev/null -w 'HTTP %{http_code} in %{time_total}s\n' http://127.0.0.1:3001/calendar"
```

`HTTP 200` 即活。配合 `docker logs aipp 2>&1 | tail -20` 看启动后有没有报错。

### 3.5 标准"测试 push"配方

想验证整条链路是否真自动，挑一处**前端可见、不改业务逻辑**的字符串改一下、push、验、改回、再 push。例如登录按钮文案 `使用 BigOffs 账号登录` 改成 `使用 BigOff 账号登录`（去 s），验证完再改回来。**不要**用 README/注释来测，因为这些不进构建产物，grep 不到。

## 4. 环境变量

- 改运行时 env：登录服务器编辑 `/opt/apps/aipp/.env`，然后 `docker restart aipp`（**不需要重新 build**）。
- 新增 env 变量：先在服务器 `.env` 补上 → 再合代码到 main。**顺序反了会导致一次失败部署**。
- 不要把 `.env` 进 git。代码里读取用 `process.env.XXX`，构建产物只内嵌 `NEXT_PUBLIC_*` 前缀的变量。

当前在用的 AI 提供商是 **Modelverse**（`MODELVERSE_API_KEY`）。OpenRouter 已下线，不要在新代码里引用。所有 AI 调用走 `src/lib/openrouter.ts` 这个适配器（名字保留，内部已切换）。

## 5. nginx 关键配置（aipp.conf）

CI 流水线会幂等更新这三条，**手改后下一次部署会被覆盖**：

```
client_max_body_size 200m;     # 上传大图（AI 输入）
proxy_read_timeout   300s;     # AI 推理可能 30-60s，避免被切
proxy_send_timeout   300s;
```

SSL 证书放在 `/etc/nginx/conf.d/cert/24103541_bigoffs.cn_nginx/`，到期前会有阿里云续签提醒，证书更新走人工 scp，不进 CI。

## 6. 应急 / 回滚

| 场景 | 做法 |
|---|---|
| 上线后发现 bug | `git revert <bad-commit> && git push origin main`，CI 会自动重新部署。**不要**直接改服务器代码。 |
| CI 挂了但需要紧急上线 | 本地跑 `bash deploy.sh`（旧脚本仍能用，等同于 CI 的 SSH 部分）。完成后**仍要**把代码 push 到 main，否则后续 CI 跑会把改动覆盖。 |
| 容器起不来 | `ssh root@47.95.109.68 "docker logs aipp 2>&1 \| tail -50"`，定位后修代码再 push。 |
| nginx 配置坏了 | `ssh root@47.95.109.68 "nginx -t"` 看报错，必要时从 `/etc/nginx/conf.d/aipp.conf.bak.<timestamp>` 还原（CI 每次会备份）。 |
| 想暂停自动部署 | 在 `.github/workflows/deploy.yml` 把 `on.push` 注释掉，或在 GitHub 仓库 Settings → Actions → Disable。 |

## 7. 不该做的事

- ❌ 直接 ssh 上去改代码 / 编 `.next` 文件。下一次 push 会全覆盖，改动丢失。
- ❌ 在本地 `npm run build` 后把 `.next` 上传。镜像里的 build 才是权威。
- ❌ 把生产 `.env` 内容贴到 PR / issue / Slack。需要分享时只贴变量名。
- ❌ 删除 `/opt/apps/aipp/data` 或 `/opt/apps/aipp/generated`。前者是用户上传的日历 xlsx，后者是缓存的海报 PNG，删了要重新生成（贵）。
- ❌ 给 `deployer` 账号加 sudo 权限。CI 不需要，扩权会扩大被攻面。
- ❌ 把个人 SSH 私钥放到 GitHub Secrets。专用密钥专用。

## 8. 给 AI 助手的提示

**接到部署/上线请求时，先把这份文档从头读一遍再动手**，不要凭记忆操作。流程必须包括：

1. 用户说"部署"、"上线"、"deploy" → 默认 `git push origin main`。不要直接 `bash deploy.sh`，除非用户明确说"应急 / 用旧脚本"。
2. **push 完立刻挂 monitor**（§3.1 的轮询脚本或 `gh run watch`），CI 跑完前不要报告"部署成功"。
3. CI 绿了之后**必须**做 §3.2-§3.4 三件事：容器 uptime 检查 + 容器内 grep 构建产物 + `/calendar` HTTP 200。不要只看 GitHub Actions 绿勾。
4. 代码里新加 `process.env.XXX` 前，先确认服务器 `.env` 已有该变量；缺就先让用户补，再合并。
5. 新 API 路由沿用 `src/lib/openrouter.ts`（Modelverse 适配器），不要引入 `openai` / `@anthropic-ai/sdk` 等 SDK。
6. 任何回滚动作只走 `git revert` + push，不要 SSH 上去改文件。
